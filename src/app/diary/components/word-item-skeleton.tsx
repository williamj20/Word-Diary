const WordItemSkeleton = () => {
  return (
    <div className="mt-3 h-12 w-full max-w-6xl animate-pulse rounded-[1.75rem] border border-[var(--brass)] bg-[var(--paper-muted)] shadow-xl sm:h-14" />
  );
};

export default WordItemSkeleton;
